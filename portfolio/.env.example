# Copy to .env.local and set the URL of your deployed reviews API.
# Leave it unset and the site falls back to the seed testimonials in
# src/data/testimonials.js — nothing breaks, the submit form just explains
# that reviews aren't connected yet.

VITE_API_URL=http://localhost:4000
